@inject NavigationManager Navigation
@using Microsoft.AspNetCore.Mvc.Localization
@inherits Frontend.Components.Pages.SearchBase

<form method="get" @onsubmit="HandleSubmitAsync">
    <input type="text" @bind="SearchText" placeholder="SearchText"/>
    <button type="submit">OK</button>
    <a href="/searchresults/@SearchText">awdawdawd</a>
</form>

@if (Profile == null)
{
<h2>Loading...</h2>
}
else
{
// <h2>@Profile.FirstName</h2>
}

@code {
private string SearchText { get; set; } = string.Empty;

private async Task HandleSubmitAsync()
{
Console.WriteLine(SearchText);
Console.WriteLine("aaaaaaaaaaaaaaaaaaaaa");

// Navigate to the search results page with the search text as a parameter
Navigation.NavigateTo($"/searchresults/{SearchText}");

await Task.CompletedTask;
}
}
